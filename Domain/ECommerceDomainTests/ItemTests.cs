using ECommerceDomain.Sales.Cart;
using Xunit;

namespace ShoppingDomainTests
{
    public class ItemTests
    {
       /* [Theory]
        [InlineData(10)]
        public void TwoEqualItems_Compared_EqualsTrue(decimal itemPrice)
        {
            var item1 = new Item(itemPrice);
            var item2 = new Item(itemPrice);

            var comparison = item1.Equals(item2);

            Assert.True(comparison);
        }*/
    }
}
